# Mailchimp credentials — required.
MAILCHIMP_API_KEY=replace-me-us21
MAILCHIMP_LIST_ID=replace-me
# Optional: only needed when the API key has no "-<dc>" suffix.
# MAILCHIMP_SERVER_PREFIX=us21
PORT=3000
LOG_LEVEL=info
